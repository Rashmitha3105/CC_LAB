> docker run --name pg-test -e POSTGRES_PASSWORD=root -d -p 5432:5432 
postgres 

> docker exec -it pg-test psql -U postgres

CREATE TABLE users ( 
id TEXT PRIMARY KEY, 
name TEXT, 
age INT 
); 
  
INSERT INTO users VALUES ('P01', 'Ganesh', 30); 
INSERT INTO users VALUES ('P02', 'Suresh', 40);

> SELECT * FROM users;
